{% extends "base.html.twig" %}
{% block body %}
<li class="list-group-item d-flex justify-content-between align-items-center">
					hello {{ user.email }} select your role
					-
					{{ user.roles|join(', ') }}
					<div class="btn-group" role="group">
						<a href="/users/{{ user.id }}/ROLE_CLIENT" class="btn btn-success">client</a>
						<a href="/users/{{ user.id }}/ROLE_FREELENCER" class="btn btn-warning">freelencer</a>
					</div>
				</li>





{% endblock %}